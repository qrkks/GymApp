# ============================================
# GymApp 生产环境配置模板
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 编辑 .env 文件，填入真实值
# 3. 设置文件权限: chmod 600 .env
# 4. 不要将 .env 文件提交到 Git
#
# 文件位置：frontend/.env
# ============================================

# ============================================
# 必需配置 - 必须填写真实值
# ============================================

# NextAuth 认证密钥
# 生成方式: openssl rand -base64 32
# 或使用: https://generate-secret.vercel.app/32
AUTH_SECRET=your-auth-secret-here

# 应用访问 URL（用于 NextAuth 回调）
# 格式: https://yourdomain.com 或 http://localhost:3000（开发环境）
NEXTAUTH_URL=https://yourdomain.com

# 域名（用于 Traefik 反向代理）
# 格式: yourdomain.com（不需要协议前缀）
DOMAIN_NAME=yourdomain.com

# PostgreSQL 数据库密码
# 请使用强密码，建议至少 16 个字符
POSTGRES_PASSWORD=your-postgres-password-here

# ============================================
# 可选配置 - 根据需要修改
# ============================================

# 数据库连接 URL（可选）
# 如果不设置，docker-compose.yml 会自动构建
# 格式: postgresql://postgres:密码@postgres:5432/gymapp
# DATABASE_URL=postgresql://postgres:your-postgres-password-here@postgres:5432/gymapp

# PostgreSQL 主机（可选，默认使用 compose 中的 postgres 服务）
# POSTGRES_HOST=postgres

# PostgreSQL 端口（可选，默认 5432）
# POSTGRES_PORT=5432

# PostgreSQL 数据库名（可选，默认 gymapp）
# POSTGRES_DB=gymapp

# PostgreSQL 用户名（可选，默认 postgres）
# POSTGRES_USER=postgres

# ============================================
# 注意事项
# ============================================
# 
# 1. 所有 your-xxx-here 的占位符必须替换为真实值
# 2. 不要使用引号包裹值（除非值本身包含空格）
# 3. 不要在值前后添加空格
# 4. 文件权限应设置为 600: chmod 600 .env
# 5. 定期备份 .env 文件到安全位置
#
# ============================================
