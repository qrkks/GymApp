name: Deploy to Production

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        load: true
        tags: gymapp-frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Save Docker image as tar
      run: |
        echo "💾 Saving Docker image to tar file..."
        docker save gymapp-frontend:latest > gymapp-image.tar

    - name: Package deploy files
      run: |
        echo "📦 Packaging deploy files..."
        # 打包 docker-compose.yml 和 .env.example
        tar -czf deploy-files.tar \
          -C frontend \
          docker-compose.yml \
          .env.example

    - name: Install sshpass
      run: |
        echo "🔧 Installing sshpass for password-based SSH..."
        sudo apt-get update
        sudo apt-get install -y sshpass

    - name: Transfer files to server
      run: |
        echo "📤 Transferring files to server..."
        sshpass -p "${{ secrets.SERVER_PASSWORD }}" scp \
          -o StrictHostKeyChecking=no \
          gymapp-image.tar \
          deploy-files.tar \
          ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:~/

    - name: Deploy on server
      run: |
        echo "🚀 Deploying on server..."
        sshpass -p "${{ secrets.SERVER_PASSWORD }}" ssh \
          -o StrictHostKeyChecking=no \
          ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
          set -e

          DEPLOY_DIR="/home/${{ secrets.SERVER_USER }}/gymapp"
          FRONTEND_DIR="$DEPLOY_DIR/frontend"

          # ============================================
          # 停止旧容器（先停止，再启动新容器）
          # ============================================
          echo "🛑 停止旧容器（如果存在）..."
          mkdir -p "$FRONTEND_DIR"
          if [ -f "$FRONTEND_DIR/docker-compose.yml" ]; then
            cd "$FRONTEND_DIR"
            docker compose down 2>/dev/null || true
            echo "✅ 旧容器已停止"
          else
            echo "ℹ️  首次部署，没有旧容器需要停止"
          fi

          # ============================================
          # 加载新镜像
          # ============================================
          echo ""
          echo "📦 Loading Docker image..."
          docker load < ~/gymapp-image.tar
          rm -f ~/gymapp-image.tar || true
          
          echo "✅ 新镜像已加载"

          echo "📁 Setting up deployment directory..."
          mkdir -p "$FRONTEND_DIR"

          echo "📥 Extracting deploy files..."
          tar -xzf ~/deploy-files.tar -C "$FRONTEND_DIR"
          rm -f ~/deploy-files.tar || true

          cd "$FRONTEND_DIR"

          # ============================================
          # .env 文件处理（首次部署）
          # ============================================
          echo ""
          echo "🔍 检查 .env 文件..."
          echo "   当前目录: $(pwd)"
          echo "   查找路径: $FRONTEND_DIR/.env"
          echo ""
          echo "📂 目录内容:"
          ls -la "$FRONTEND_DIR" | head -20 || true
          echo ""
          
          # 检查 .env 文件是否存在
          if [ ! -f .env ]; then
            echo "⚠️  .env 文件不存在于预期位置: $FRONTEND_DIR/.env"
            echo ""
            echo "🔍 搜索其他位置的 .env 文件..."
            find "$DEPLOY_DIR" -name ".env" -type f 2>/dev/null | head -5 || echo "   未找到其他 .env 文件"
            echo ""
            echo "⚠️  正在创建模板..."
            
            if [ -f .env.example ]; then
              cp .env.example .env
              chmod 600 .env
              echo "✅ 已从 .env.example 创建 .env 文件"
              echo ""
              echo "❌ 部署失败：.env 文件包含模板占位符"
              echo ""
              echo "📋 请按以下步骤操作："
              echo "   1. SSH 到服务器: ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}"
              echo "   2. 进入目录: cd $FRONTEND_DIR"
              echo "   3. 编辑 .env 文件: nano .env"
              echo "   4. 填入真实值（替换所有 'your-xxx-here' 占位符）"
              echo "   5. 保存后重新运行部署或手动启动: docker compose up -d"
              echo ""
              echo "📝 必需的环境变量："
              echo "   - AUTH_SECRET: NextAuth 认证密钥"
              echo "   - POSTGRES_PASSWORD: PostgreSQL 数据库密码"
              echo "   - NEXTAUTH_URL: 应用 URL (如: https://yourdomain.com)"
              echo "   - DOMAIN_NAME: 域名 (如: yourdomain.com)"
              echo ""
              exit 1
            else
              echo "❌ 错误：.env.example 文件不存在"
              exit 1
            fi
          else
            echo "✅ 找到 .env 文件: $FRONTEND_DIR/.env"
            echo "   文件大小: $(stat -c%s .env 2>/dev/null || stat -f%z .env 2>/dev/null || echo 'unknown') 字节"
            echo "   文件权限: $(ls -l .env | awk '{print $1}')"
            echo ""
          fi

          # 验证 .env 文件是否包含占位符
          echo "🔍 验证 .env 文件内容..."
          if grep -q "your-.*-here\|请替换" .env 2>/dev/null; then
            echo ""
            echo "❌ 部署失败：.env 文件包含模板占位符"
            echo ""
            echo "📋 发现以下占位符:"
            grep -n "your-.*-here\|请替换" .env 2>/dev/null | head -10 || true
            echo ""
            echo "📋 请按以下步骤操作："
            echo "   1. SSH 到服务器: ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}"
            echo "   2. 进入目录: cd $FRONTEND_DIR"
            echo "   3. 编辑 .env 文件: nano .env"
            echo "   4. 填入真实值（替换所有 'your-xxx-here' 占位符）"
            echo "   5. 保存后重新运行部署或手动启动: docker compose up -d"
            echo ""
            exit 1
          fi

          # 确保 .env 文件权限正确
          chmod 600 .env

          echo "✅ .env 文件验证通过"

          # ============================================
          # 部署服务
          # ============================================
          echo ""
          echo "🐳 启动 Docker Compose 服务..."
          
          # 验证 Docker 镜像是否存在
          if ! docker images | grep -q gymapp-frontend; then
            echo "❌ Docker 镜像未找到"
            exit 1
          fi

          # 使用 docker compose 启动服务
          # .env 文件会自动加载（通过 env_file 配置）
          echo ""
          echo "🚀 启动新容器..."
          docker compose up -d

          echo ""
          echo "⏳ 等待容器启动（应用可能需要时间完成数据库迁移）..."
          sleep 10

          echo ""
          echo "📊 容器状态:"
          docker compose ps

          # ============================================
          # 简单检查：只检查容器是否在运行
          # ============================================
          echo ""
          echo "🔍 检查容器状态..."
          
          if docker ps | grep -q gymapp-frontend; then
            echo "✅ 容器正在运行"
            
            # 显示容器详细信息
            echo ""
            echo "📋 容器详细信息:"
            docker ps --filter "name=gymapp-frontend" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
            
            # 显示应用启动日志（最后 30 行）
            echo ""
            echo "📝 应用启动日志（最后 30 行）:"
            docker compose logs gymapp --tail 30 || true
            
            echo ""
            echo "✅ 容器已启动，应用正在运行中..."
            echo "💡 提示：如果应用还在运行数据库迁移，请稍等片刻"
            echo "💡 查看实时日志: docker compose logs -f gymapp"
            
          else
            echo "❌ 容器未运行"
            echo ""
            echo "📝 查看所有日志:"
            docker compose logs
            echo ""
            echo "❌ 部署失败：容器启动失败"
            exit 1
          fi

          # ============================================
          # 清理旧镜像和未使用的资源
          # 注意：只有在健康检查通过后才会执行此步骤
          # 如果健康检查失败，旧镜像和容器会保留，可用于回滚
          # ============================================
          echo ""
          echo "🧹 清理旧镜像和未使用的资源（新容器已验证正常运行）..."
          
          # 显示清理前的镜像信息
          echo "📊 清理前的镜像:"
          docker images gymapp-frontend --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.Size}}\t{{.CreatedAt}}" 2>/dev/null || true
          
          # 清理悬空镜像（dangling images）- 这些是旧版本的镜像，标签已被新镜像替换
          DANGLING_IMAGES=$(docker images -f "dangling=true" -q 2>/dev/null || true)
          if [ -n "$DANGLING_IMAGES" ]; then
            echo "🗑️  发现悬空镜像，正在清理..."
            docker rmi $DANGLING_IMAGES 2>/dev/null || echo "   部分悬空镜像可能仍在使用中，跳过"
          else
            echo "✅ 没有悬空镜像需要清理"
          fi
          
          # 清理旧的 gymapp-frontend 镜像（保留最新的 2 个版本，以便回滚）
          # 获取所有 gymapp-frontend 镜像，按创建时间排序，删除最旧的
          OLD_IMAGES=$(docker images gymapp-frontend --format "{{.ID}}" 2>/dev/null | tail -n +3 || true)
          if [ -n "$OLD_IMAGES" ]; then
            echo "🗑️  发现旧版本的 gymapp-frontend 镜像，正在清理（保留最新 2 个版本）..."
            for IMAGE_ID in $OLD_IMAGES; do
              # 检查镜像是否正在被容器使用
              if ! docker ps -a --format "{{.Image}}" 2>/dev/null | grep -q "$IMAGE_ID"; then
                echo "   删除旧镜像: $IMAGE_ID"
                docker rmi "$IMAGE_ID" 2>/dev/null || echo "   无法删除镜像 $IMAGE_ID（可能仍在使用中）"
              else
                echo "   跳过正在使用的镜像: $IMAGE_ID"
              fi
            done
          else
            echo "✅ 没有旧版本的镜像需要清理"
          fi
          
          # 清理未使用的构建缓存（节省空间）
          echo "🧹 清理未使用的构建缓存（24小时前）..."
          docker builder prune -f --filter "until=24h" 2>/dev/null || true
          
          # 显示最终的镜像信息
          echo ""
          echo "📊 最终的镜像列表:"
          docker images gymapp-frontend --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.Size}}\t{{.CreatedAt}}" 2>/dev/null || true
          
          # 显示最终的磁盘使用情况
          echo ""
          echo "💾 最终 Docker 磁盘使用情况:"
          docker system df 2>/dev/null || true

          echo ""
          echo "✅ 部署完成！"
        EOF
