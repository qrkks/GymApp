# 环境变量配置模板
# 复制此文件为 .env.local (开发环境) 或在生产环境中设置这些变量

# NextAuth 认证密钥 (必需)
# 生成命令: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here-replace-with-actual-secret

# 数据库文件路径
# 生产环境建议使用绝对路径
DATABASE_PATH=./db.production.sqlite3

# NextAuth URL (生产环境必需)
# 必须是完整的URL，包括协议
NEXTAUTH_URL=https://yourapp.com

# 生产环境已设置的环境变量 (无需手动设置)
# NODE_ENV=production
# AUTH_TRUST_HOST=true
