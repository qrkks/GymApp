# 部署指南

## 环境要求

- Docker & Docker Compose
- Node.js 22.x（用于本地开发）

## 快速部署

### 1. 环境变量配置

复制环境变量模板并配置：

```bash
# 复制环境变量模板
cp env.example.txt .env

# 编辑环境变量（必需）
# 生成AUTH_SECRET: openssl rand -base64 32
AUTH_SECRET=your-generated-secret-key
DATABASE_PATH=./db.production.sqlite3
NEXTAUTH_URL=https://yourdomain.com
```

### 2. 启动应用

使用Docker Compose启动：

```bash
# 构建并启动服务（自动初始化数据库）
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f gymapp
```

**注意**: 应用启动时会自动检查并初始化数据库，无需手动执行数据库初始化命令。

## 第一次部署详情

### 自动数据库初始化流程

1. **容器启动时检查**:
   - 检查数据库文件是否存在 (`/app/db/db.production.sqlite3`)
   - 如果不存在，SQLite会自动创建空数据库文件

2. **表结构检查**:
   - 检查数据库是否有表结构
   - 如果没有表，自动运行初始化脚本

3. **优化配置应用**:
   - 启用WAL模式
   - 配置性能优化参数

### 验证第一次部署

```bash
# 1. 查看启动日志
docker-compose logs gymapp

# 应该看到类似输出:
# 📊 检查数据库状态...
# ⚠️  数据库文件不存在，开始初始化...
# ✅ 数据库初始化完成
# 🔧 应用数据库优化配置...
# 🎯 启动应用服务器...

# 2. 检查数据库表
docker-compose exec gymapp sqlite3 /app/db/db.production.sqlite3 ".tables"

# 应该看到:
# body_parts  sets  users  workout_body_parts  workout_sets  workouts

# 3. 测试API
curl http://localhost:3000/api/health
```

### 如果初始化失败

如果数据库初始化失败，容器会启动失败。检查日志：

```bash
# 查看详细错误日志
docker-compose logs gymapp

# 常见问题：
# 1. 权限问题 - 检查volume挂载权限
# 2. 环境变量缺失 - 检查AUTH_SECRET等
# 3. 磁盘空间不足 - 检查可用磁盘空间
```

### 3. 数据库初始化

首次启动后初始化数据库：

```bash
# 进入容器
docker-compose exec gymapp sh

# 初始化数据库
cd /app
pnpm run db:init

# 退出容器
exit
```

## 部署选项

### 选项1: Docker Compose（推荐）

```yaml
# docker-compose.yml 已配置包含：
# - 自动重启
# - 健康检查
# - 数据持久化
# - 网络隔离
```

### 选项2: 直接Docker命令

```bash
# 构建镜像
docker build -t gymapp ./frontend

# 运行容器
docker run -d \
  --name gymapp \
  -p 3000:3000 \
  -e AUTH_SECRET="your-secret" \
  -e DATABASE_PATH="/app/db.production.sqlite3" \
  -e NEXTAUTH_URL="https://yourdomain.com" \
  -v $(pwd)/db:/app/db \
  gymapp
```

### 选项3: 使用外部数据库

修改环境变量使用外部SQLite文件：

```bash
# 使用主机目录挂载
DATABASE_PATH=/host/db/production.sqlite3
```

## 维护命令

### 备份数据库

```bash
# 停止服务
docker-compose down

# 备份数据库
cp db/production.sqlite3 db/backup-$(date +%Y%m%d).sqlite3

# 重启服务
docker-compose up -d
```

### 更新应用

```bash
# 重新构建镜像
docker-compose build --no-cache

# 重启服务
docker-compose up -d
```

### 查看健康状态

```bash
# 健康检查API
curl http://localhost:3000/api/health

# Docker健康状态
docker-compose ps
docker stats
```

## 生产环境配置

### 反向代理（Nginx）

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### SSL证书（Let's Encrypt）

```bash
# 使用certbot
certbot --nginx -d yourdomain.com
```

### 环境变量安全

- 不要在代码中硬编码密钥
- 使用强密码作为AUTH_SECRET
- 定期轮换密钥
- 限制环境变量文件权限

## 故障排除

### 常见问题

1. **端口冲突**
   ```bash
   # 检查端口占用
   netstat -tulpn | grep :3000
   # 修改docker-compose.yml中的端口映射
   ```

2. **数据库权限问题**
   ```bash
   # 确保db目录权限正确
   chmod 755 db/
   ```

3. **内存不足**
   ```bash
   # 检查容器资源使用
   docker stats
   # 增加内存限制（在docker-compose.yml中）
   deploy:
     resources:
       limits:
         memory: 512M
   ```

### 日志分析

```bash
# 查看应用日志
docker-compose logs gymapp

# 查看错误日志
docker-compose logs gymapp | grep -i error

# 实时监控
docker-compose logs -f gymapp
```

## 监控和告警

### 健康检查端点

- GET `/api/health` - 基础健康检查
- 返回应用状态、运行时间、版本信息

### 建议监控项

- 容器健康状态
- 内存使用率
- CPU使用率
- 磁盘空间
- 数据库连接状态

---

**最后更新：** 2026年1月2日
